{% extends 'base.html.twig' %}

{% block title %}Liste des paniers{% endblock %}

{% block body %}
<h2 class="text-center mt-4">{{'listUser.titre'|trans}}</h2>
<table class="table table-striped">
    <thead>
        <th>ID</th>
        <th>Pseudo</th>
        <th>Nom</th>
        <th>Prénom</th>
        <th>Email</th>
        <th>Role</th>
    </thead>
    <tbody>
        {% for u in allUser %}
        <tr>
            <td>{{u.id}}</td>
            <td>{{u.username}}</td>
            <td>{{u.nom}}</td>
            <td>{{u.prenom}}</td>
            <td>{{u.email}}</td>
            <td>{{u.roles[0]}}</td>
        </tr>


        {% endfor %}
    </tbody>
</table>
{% endblock %}